felonvm - vm in pure C code

features:
 - simple code
 - fast execution
 - light api

syntax:
  operation-to-from
example:
  move      R0 10

registers (current num = 4):
  R + 0-3
  (R0, R1, ..., R3)